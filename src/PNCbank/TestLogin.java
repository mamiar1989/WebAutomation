package PNCbank;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.Test;

public class TestLogin {

/*
    WebDriver driver;
    String personalBanking = "\"//*[@id=\\\"mast-nav__links\\\"]/div/div/div[1]/ul/li[1]/a/span\"";
    String pncUrl = "https://www.pnc.com/en/personal-banking.html";
    String smallBuisnesses="//*[@id=\"mast-nav__links\"]/div/div/div[1]/ul/li[1]/a/span";
    String anouncement="document.querySelector(\"#sb-c-mm-bdda-checking-200offer > div > div > div.description.left-justified > p > b\")";


    @BeforeMethod
    public void setUp() {

        String chromeDriverPath = "BrowserDriver/windows/chromedriver.exe";   // so wecreate a setup method to execute before every test case
        System.setProperty("webdriver.chrome.driver", chromeDriverPath);// we dont want to set the values everytime
        WebDriver driver = new ChromeDriver();// create a variable named WebDriver
        driver.manage().window().fullscreen();
        driver.manage().deleteAllCookies();
        driver.get(pncUrl);
    }


    public void Personal() {
        Personal();
        driver.findElement(By.xpath(smallBuisnesses)).click();
    }


    @Test
    public void testPersonal() {
        Personal();
        String expectResult = "When you open and use\n" +
                "a new, qualifying\n" +
                "PNC business checking account.";
        String actualRerult = driver.findElement(By.xpath(anouncement)).getText();


    }

*/
}









